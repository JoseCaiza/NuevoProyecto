CREATE TABLE `cloudcomputing`.`alumno` (
  `id` INT NOT NULL AUTO_INCREMENT,
  `identificacion` VARCHAR(45) NULL,
  `nombre` VARCHAR(45) NULL,
  `edad` INT NULL,
  `telefono` VARCHAR(45) NULL,
  PRIMARY KEY (`id`));
